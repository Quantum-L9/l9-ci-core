"""L9 CI Core — bootstrap validators and the declarative gate control plane.

This is an installable package (src layout). Install for development with::

    python -m pip install --no-deps -e .

after installing the locked bootstrap profile
(``requirements/bootstrap.lock``). The control plane lives under
:mod:`l9_ci_core.control_plane`.
"""

__version__ = "1.0.0"
