on: push
jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - run: |
          # l9-download: gitleaks-linux-x64
          curl --fail "https://github.com/gitleaks/gitleaks/releases/latest/download/gitleaks.tar.gz" -o /tmp/gl.tar.gz
          sha256sum --check <<< "$SHA  /tmp/gl.tar.gz"
