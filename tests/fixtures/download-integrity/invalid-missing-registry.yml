on: push
jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: unregistered
        run: |
          # l9-download: unregistered-key
          curl --fail "$URL" --output "$RUNNER_TEMP/tool.tar.gz"
          sha256sum --check <<< "$SHA  $RUNNER_TEMP/tool.tar.gz"
