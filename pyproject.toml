[project]
name = "l9-ci-core-validation"
version = "2.0.0.dev1"
description = "Validation harness for the l9-ci-core thin control plane"
requires-python = ">=3.11"
[tool.ruff]
line-length = 88
target-version = "py311"
[tool.pytest.ini_options]
testpaths = ["tests"]
