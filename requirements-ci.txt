# CI / local-gate dependencies for l9-ci-core's own Python control-plane code.
#
# Installed by Core `self-ci.yml` lint job (and local `make check`) before
# ruff/mypy run — not a phantom lint-test workflow.
#
# The only third-party import across .github/actions/ and tools/ is `yaml`
# (PyYAML). types-PyYAML supplies the mypy stubs so the type gate resolves
# `import yaml` without falling back to `--ignore-missing-imports`.
PyYAML==6.0.3
types-PyYAML==6.0.12.20260724
