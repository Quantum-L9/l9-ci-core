name: Validate Core governance
description: >
  Validates every supported profile and provider combination in the governance
  bundle shipped with Core.
runs:
  using: composite
  steps:
    - shell: bash
      run: python3 "${{ github.action_path }}/validate_governance.py"
