{
  "schema": "l9.quality-threshold-selection/v1",
  "profiles": {
    "pr_fast": {"sdk_policy": "semgrep-policy.yaml"},
    "merge": {"sdk_policy": "semgrep-policy.yaml"},
    "nightly": {"sdk_policy": "semgrep-policy.yaml"},
    "release": {"sdk_policy": "semgrep-policy.yaml"},
    "supply_chain": {"sdk_policy": "semgrep-policy.yaml"}
  },
  "note": "sdk_policy names the Core-bundled advisory policy (sibling of defaults/). resolve_policy stages it into the consumer workspace so invoke-sdk's workspace jail can pass --policy. Consumers cannot replace this file."
}
