{
  "schema": "l9.rule-modes/v1",
  "defaults": {
    "pr_fast": "blocking",
    "merge": "blocking",
    "nightly": "advisory",
    "release": "blocking",
    "supply_chain": "blocking"
  },
  "provider_overrides": {
    "semgrep": {
      "pr_fast": "advisory",
      "merge": "advisory"
    }
  },
  "allowed_modes": [
    "blocking",
    "advisory",
    "shadow",
    "disabled"
  ]
}
