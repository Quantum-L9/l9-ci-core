# --- L9_META ---
# l9_schema: 1
# origin: l9-ci-core
# layer: [ci, lint-config]
# tags: [L9_CI, ruff, self-ci]
# owner: platform
# status: active
# --- /L9_META ---
#
# Ruff config for l9-ci-core's own Python (governance script + tests). Only sets
# the line length so `ruff format` is stable at 100 cols; lint rule selection is
# left at ruff defaults (E4/E7/E9/F) — this repo's Python is small utility code,
# not the SDK's strict surface.
line-length = 100
